const axios = require("axios").default;

const getPrice = async (cryptocurrencyId, currencies) => {
  // simple/price
};

const getAditionalInfo = async (cryptocurrencyId, currency) => {
  //coins/markets
};

module.exports = { getPrice, getAditionalInfo };
